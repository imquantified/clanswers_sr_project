<%= form_for @thoughtful_question do |f| %>
	<%= render 'shared/error_messages', object: f.object %>
	<div class="thoughtful_question_field">
		<%= f.text_area :content, placeholder: "Ask a thoughtful question..." %>
	</div>
	<%= f.submit "Post", class: "btn btn-large btn-primary" %>
<% end %>