<% provide(:title, @user.username) %>

<div class="row">
	<aside class="col-sm-4">
		<section>
			<h1>
				<%= gravatar_for @user %>
				<b><%= @user.username %></b>
			</h1>
			<h2>
				<%= @user.email %>
			</h2>
		</section>
	</aside>
</div>