<% provide(:title, @user.username) %>

<div class="row">
	<aside class="span4">
		<section>
			<h1>
				<%= gravatar_for @user %>
				<b><%= @user.username %></b>
			</h1>
			<h2>
				<%= @user.email %>, <%= @user.first_name %> <%= @user.last_name %>
			</h2>
		</section>
	</aside>
</div>